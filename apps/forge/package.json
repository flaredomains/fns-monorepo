{
  "name": "@fns/forge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "forge build",
    "test": "forge test",
    "clean": "forge clean",
    "coverage": "forge coverage",
    "format": "forge fmt",
    "deploy": "forge script script/DeployFNS.s.sol --rpc-url=$COSTON2_RPC --verify --verifier=blockscout --verifier-url='https://coston2-explorer.flare.network/api?module=contract&action=verifysourcecode' --retries 10 --delay 30 --broadcast -vvvvv"
  }
}